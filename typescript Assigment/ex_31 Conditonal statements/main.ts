
let users: Array<string> = ["Owaiskhilji", "AdeenaAsghar", "Admin", "user420", "unknownuser"]
if(users.length === 0) {
    console.log("We need to find new users!")
}

let new_users: Array<any> = []
if(new_users.length === 0) {
    console.log("Wev need to find new users!")
}